# Codebase RAG Assistant

Локальный RAG-ассистент (Retrieval-Augmented Generation) для работы с кодовой базой с приоритетом приватности. Построен на Angular с использованием локальных LLM (через LM Studio) или Gemini API.

## 🚀 Возможности

- **Общение с кодом**: Задавайте вопросы о структуре проекта, логике и паттернах кода.
- **Локальный RAG**: Использует локальное векторное хранилище (IndexedDB) для индексации кода прямо в браузере. Данные не покидают ваше устройство, если не используются облачные LLM.
- **Векторный поиск**: Семантический поиск на основе эмбеддингов.
- **Несколько провайдеров**: Поддержка:
  - **Gemini API** (Google)
  - **Локальные LLM** (через LM Studio / OpenAI-совместимые эндпоинты)
- **Speech функциональность**:
  - **Speech-to-Text (STT)**: Распознавание голоса для ввода текста через Web Speech API, OpenAI Whisper или Groq
  - **Text-to-Speech (TTS)**: Озвучивание ответов ассистента через Web Speech API, OpenAI или ElevenLabs
- **Инструкции проекта**: Автоматическая генерация инструкций для папок и файлов проекта с возможностью редактирования и регенерации
- **Экспорт чата**: Экспорт истории диалогов в формат Markdown
- **Быстрые пресеты**: Готовые вопросы для быстрого старта (о проекте, структуре, запуске и т.д.)
- **Отображение источников**: Показывает источники, использованные для генерации ответа

## 🛠️ Технологический стек

- **Frontend**: Angular 21+ (с Zoneless Change Detection и Signals)
- **Язык**: TypeScript 5.9
- **Сборка**: Angular CLI (esbuild)
- **Деплой**: Docker + Nginx

## 📂 Структура проекта

- `src/main.tsx` - Точка входа в приложение
- `src/app.component.ts` - Корневой компонент с поддержкой drag-and-drop
- `src/features/` - UI-компоненты функциональности:
  - `chat/` - Чат-интерфейс с поддержкой RAG
  - `sidebar/` - Проводник файлов с возможностью поиска
  - `settings/` - Настройки провайдеров и эмбеддингов
  - `project-instructions/` - Управление инструкциями проекта
- `src/core/` - Основная логика:
  - `services/` - Синглтон-сервисы (RagService, LlmService, VectorStoreService и др.)
  - `models/` - TypeScript модели
  - `constants/` - Константы
  - `utils/` - Утилиты
- `src/shared/` - Переиспользуемые UI-компоненты и пайпы
- `src/services/` - Бизнес-сервисы (устаревающая директория, планируется миграция в core/)

## 🏁 Начало работы

### Предварительные требования

- Node.js 20+
- Docker (опционально, для контейнеризированного запуска)

### 1. Установка

```bash
npm install
```

### 2. Конфигурация (`.env`)

Для локальной разработки или Docker-деплоя создайте файл `.env`:

```env
APP_PORT=3000
```

> **Примечание**: API-ключи управляются через LocalStorage браузера или интерфейс настроек, а не через `.env` для безопасности.

### 3. Запуск локально

```bash
npm start
# Приложение будет доступно по адресу http://localhost:4200
```

### 4. Запуск с Docker

Используйте встроенный скрипт деплоя:

```bash
# Сборка и запуск
./deploy.sh up

# Остановка
./deploy.sh down

# Просмотр логов
./deploy.sh logs

# Перезапуск
./deploy.sh restart

# Полная очистка
./deploy.sh clean
```

Приложение будет доступно по адресу `http://localhost:3000` (или указанному в `APP_PORT`).

## 📖 Использование

### Загрузка файлов

1. Перетащите файлы или папки в приложение (drag-and-drop)
2. Или используйте проводник файлов в боковой панели
3. Файлы автоматически индексируются и добавляются в векторное хранилище

### Настройка LLM

Откройте настройки (иконка ⚙️) и выберите провайдер:

1. **Gemini API**:
   - Укажите API ключ Google AI
   - Выберите модель (например, `gemini-2.0-flash-exp`)
   - Настройте параметры генерации

2. **Локальный LLM** (LM Studio):
   - Запустите LM Studio
   - Включите сервер API
   - Укажите URL эндпоинта (по умолчанию `http://localhost:1234/v1`)
   - Выберите модель

### Настройка эмбеддингов

Для RAG-поиска необходимы эмбеддинги:

1. **Gemini API**: Использует те же ключи, что и чат
2. **Локальный**: Настройте отдельный эндпоинт для эмбеддингов

### Работа с чатом

1. Введите вопрос в поле ввода
2. Включите/выключите контекст RAG переключателем
3. Используйте быстрые пресеты для типичных вопросов
4. Экспортируйте диалог в Markdown при необходимости

### Инструкции проекта

1. Откройте модуль инструкций проекта (иконка 📚)
2. Сгенерируйте инструкции для всех папок и файлов
3. Редактируйте инструкции для улучшения контекста
4. Регенерируйте отдельные элементы при необходимости

## 🧠 Архитектура

Подробное описание плана рефакторинга и архитектурных решений см. в файле [.gemini/project-instructions.md](.gemini/project-instructions.md).

Ключевые архитектурные особенности:
- **Zoneless Change Detection**: Отключена зональная детекция изменений для улучшения производительности
- **Signals**: Использование Angular Signals для реактивного состояния
- **IndexedDB**: Локальное хранение векторов для полной автономности
- **Standalone Components**: Все компоненты являются standalone

## 🧪 Тестирование

```bash
# Запуск unit-тестов
npm test

# Запуск с покрытием
npm run test -- --code-coverage
```

## 📝 Лицензия

Private / Internal Use.
